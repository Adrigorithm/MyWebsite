"use strict"

const WriteMode = {
    SET: "SET",
    APPEND: "APPEND"
};

const RequestMethod = {
    GET: "GET",
    POST: "POST"
};

const ContentType = {
    JSON: "application/json"
};

export {WriteMode, RequestMethod, ContentType};
